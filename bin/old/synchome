#!/bin/bash

FILES_TO_SYNC=(.bashrc .bash_aliases bin .ssh/config)

REMOTE_LOCATIONS=(dsgci-3.euhpc.arm.com man-01.manchester.arm.com)

cd
for l in ${REMOTE_LOCATIONS[@]}
do
  for f in ${FILES_TO_SYNC[@]}
  do
    rsync -rv $f $l:$f
  done
done